## Инициализация
```js
ctx.pattern(image, [offsetX, offsetY, repeat]);

ctx.pattern({
	image, offsetX, offsetY, repeat
});

Delta.pattern(...);
```

## Параметры
 - `offsetX`
 - `offsetY`
 - `width`
 - `height`
 - `repeat` = true / false / 'x' / 'y'